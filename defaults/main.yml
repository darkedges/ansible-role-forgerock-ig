---

install_root: /opt/ois
staging_dir: /tmp/staging
frig_fr_user: forgerock

frig_version: "6.5.0"
frig_version_eval: true
frig_install_file: "IG{{ '-eval' if frig_version_eval  }}-{{ frig_version }}.war"

tomcat_service: frig
tomcat_instance_dir: "{{ install_root }}/{{ tomcat_service }}"
